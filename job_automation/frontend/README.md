# Job Automation frontend

Single-screen React and Vite interface for the local FastAPI backend.

```powershell
npm.cmd install
npm.cmd run dev
```

The API defaults to `http://127.0.0.1:8000`. Override it with `VITE_API_URL` when needed.
